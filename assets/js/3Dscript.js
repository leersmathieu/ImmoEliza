document.addEventListener("DOMContentLoaded", () => {
  import * as THREE from "../node_modules/three/build/three";
  import * as THREE from "../node_modules/three-ply-loader/index";

  const scene = new THREE.Scene();
});
